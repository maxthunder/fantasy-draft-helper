# Environment Variables Configuration
# Copy this file to .env and update with your values

# Database Configuration
# For local development:
DATABASE_URL=postgresql://localhost:5432/fantasy_draft_helper

# For production (Render), use the External Database URL from Render dashboard:
# DATABASE_URL=postgresql://user:password@host.oregon-postgres.render.com/database_name

# Server Configuration
PORT=8081
NODE_ENV=development

# On Render, these are set automatically:
# NODE_ENV=production
# PORT is assigned by Render
# RENDER=true (automatically set by Render)